# Base URL the frontend uses to reach the API in production builds.
# In local dev, Vite's dev-server proxy (vite.config.ts) forwards /api to the
# backend instead — this variable isn't consulted there.
VITE_API_BASE_URL=http://localhost:3000
