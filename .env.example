# Root-level env for `docker compose up`. Copy to .env before running.
# Per-package .env.example files (backend/, frontend/) document the same
# variables for running each package outside Docker.

JWT_ACCESS_SECRET=dev_only_access_secret_change_me
JWT_REFRESH_SECRET=dev_only_refresh_secret_change_me
